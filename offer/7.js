function rebuild(perOrder, inOrder, length) {
    if (perOrder == null || inOrder == null || length <= 0) return null;
    return rebuildCore(perOrder, inOrder, )
}

function rebuildCore(perOrder, inOrder, ) {
    var root = perOrder[0];
    var node = new Node(root.val);
}